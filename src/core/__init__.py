"""Core business logic. Pure Python — no Qt, no SQL, no I/O.

This layer knows nothing about how it is invoked (GUI) or where data
lives (db). It only transforms values. That makes it trivially testable.
"""
